{
  "name": "lethe-7",
  "version": "0.1.0",
  "private": true,
  "description": "LETHE-7 — an original low-poly retro sci-fi survival horror vertical slice for the browser.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "start": "npx serve dist -l 8080"
  },
  "license": "MIT",
  "dependencies": {
    "three": "^0.186.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
